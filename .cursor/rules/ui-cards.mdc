---
description: 卡片圆角和间距与「我的」页面保持一致
alwaysApply: true
---

# 卡片样式

新界面的白卡片对齐 `pages/profile/profile.wxss`。

- 页面背景 `#f5f5f5`，左右留白 `24rpx`
- 白卡片圆角 `48rpx`，卡片之间间距 `24rpx`
- 卡片内行内边距 `24rpx 32rpx`
- 同一张卡片里的多行用 `1rpx` 浅分割线分开，最后一行不加线
- 行标题 `28rpx`、`#222222`
